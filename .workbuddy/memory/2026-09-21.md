# 2026-09-21

## Day 5（第 1 周）
- 板块① 三层分工讲完（前端=唯一主角、数据文件=唯一源头、本地缓存=备份、后端+数据库本期虚线缺位），用户复述过关。
- 板块② 技术路线拍板：方案 A 纯静态三件套（HTML/CSS/JS + data.js + GitHub Pages；无框架无构建）。备选 B（React/Vite + CloudBase）写明暂不采用理由，Day 23 数据库时后端可按 B 补入。用户未确认课程是否有示范路线，已留口。
- 板块③ 产出 data-flow.svg（实线主链：站长→data.js→index.html→用户；虚线：断网缓存兜底）+ TECH_DESIGN.md v1.0（路线一行版、理由、项目结构、数据模型 rank/title/heat/source/url + date、数据流、PRD 对应表）。
- 用户思考题复述：「从站长手动整理来，到页面榜单上去」，过关。
- 收尾：commit 9502278（TECH_DESIGN.md + data-flow.svg），已推送。
- 【注意】git status 显示 PRD.md / research.md 被外部 Markdown 编辑器重排了表格对齐（纯格式、无内容变化），未提交，待用户决定：随 Day 6 提交或恢复原样。
- 【环境】bash 外壳 PATH 损坏问题仍在，继续用 export PATH 前缀 PortableGit cmd/usr/bin/mingw64/bin 的方式调用 git。
